services:

  # Defines a simple service which requires no parameter for its constructor.
  forecast.GenericService:
    class: Drupal\survey\Service\GenericService
    arguments: ['@messenger', '@current_user']

parameters:
  forecast.parameter: TRUE