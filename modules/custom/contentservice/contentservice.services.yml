services:

  # Defines a simple service which requires no parameter for its constructor.
  contentservice.GenericService:
    class: Drupal\contentservice\Service\GenericService
    arguments: ['@messenger', '@current_user']

parameters:
  contentservice.parameter: TRUE